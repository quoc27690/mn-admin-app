import * as React from "react";
import { Text, View } from "react-native";

function PHHSScreen({ navigation, route }) {
	return (
		<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
			<Text>PHHSScreen</Text>
		</View>
	);
}

export default PHHSScreen;
